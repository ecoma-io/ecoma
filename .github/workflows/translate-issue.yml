name: Issue translation

on:
  issues:
    types: [opened, edited]
  workflow_dispatch:
    inputs:
      number:
        description: Issue number to (re-)translate
        required: true

# A rapid title/body edit supersedes the previous translation -- only the
# newest text is worth spending free-tier calls on.
concurrency:
  group: translate-issue-${{ github.event.issue.number || inputs.number }}
  cancel-in-progress: true

permissions:
  contents: read
  issues: write

jobs:
  translate:
    # No author guard here, deliberately, and unlike issue-triage: a reader who
    # works in Vietnamese reads a bot's issue too, and Renovate's Dependency
    # Dashboard is the one issue in the repository that is always open. What
    # made a bot thread look unaffordable was the repetition -- it is rewritten
    # whenever any Renovate pull request changes state, and every rewrite is an
    # `edited` event -- so repo-care stamps the source digest on its own comment
    # and returns before the first model call when nothing it renders from has
    # changed. Excluding the author would have cured the symptom by dropping the
    # translation.
    runs-on: ubuntu-latest
    # One detection quorum plus one call per target language, on a slow free
    # tier -- well under the multi-turn budget pr-practice-review needs.
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .node-version

      # repo-care is deliberately dependency-free (bare node, global fetch),
      # so no pnpm install.
      - name: Translate the issue
        run: node shared/tools/repo-care/src/main.mjs translate-issue --issue "$NUMBER"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          NUMBER: ${{ github.event.issue.number || inputs.number }}
