# Generated by `tauri build`/`tauri dev` from tauri.conf.json and the plugin
# ACL — a derived artifact, regenerated on every run. Committing it would put a
# build output under review and make a clean tree depend on when you last built.
/gen/schemas/
