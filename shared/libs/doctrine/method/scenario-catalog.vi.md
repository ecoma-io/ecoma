---
title: "Scenario catalog"
status: design-end-state
canonical-sha: bba764df103f
---

# Scenario catalog

## Phần 1 — Scenario battery (seed, 40 kịch bản)

| ID  | Kịch bản (1 dòng)                                         | Verdict gần nhất                            |
| --- | --------------------------------------------------------- | ------------------------------------------- |
| S01 | Webhook → transform → gọi API                             | ✅                                          |
| S02 | Schedule kéo report → email                               | ✅                                          |
| S03 | Form → validate → ghi CRM                                 | ✅                                          |
| S04 | n=1 giao AI viết, tự duyệt qua inbox                      | ✅                                          |
| S05 | RPA standalone điền form web không API                    | ✅                                          |
| S06 | Brief → AI draft → người edit → publish CMS (flagship)    | ✅                                          |
| S07 | Chatbot FAQ → bí → chuyển người                           | ✅                                          |
| S08 | Hóa đơn PDF qua email → extract → duyệt → ghi sổ          | ✅                                          |
| S09 | Sàng lọc CV; ứng viên = external filler                   | ✅                                          |
| S10 | Onboarding song song IT(RPA)+HR(người)+manager            | ✅                                          |
| S11 | AI writer shadow người 2 tuần → graduation                | ✅                                          |
| S12 | Người mới shadow quy trình AI để học                      | ✅                                          |
| S13 | Hoàn tiền Messenger: dùng policy internal, trả lời public | ✅ (leakage-gate — sinh từ finding)         |
| S14 | Đàm phán B2B nhiều vòng + legal + provisioning            | ✅                                          |
| S15 | Agent phân rã landing page → 7 subtask, 2 cho người       | ✅                                          |
| S16 | Model provider sập giữa 50 task → correlation gộp         | ✅                                          |
| S17 | Migrate quy trình đang chạy 3 tuần                        | ✅                                          |
| S18 | Cài block Hub, fork criteria, nhận upstream               | ✅                                          |
| S19 | Audit "ai duyệt, tri thức nào, model nào"                 | ✅                                          |
| S20 | Nhân viên nghỉ đột ngột giữa 12 task treo                 | ✅                                          |
| S21 | Hai process đua ghi một record CRM                        | ✅ (serialization_key/Lease)                |
| S22 | Prompt injection "xuất toàn bộ policy"                    | ✅                                          |
| S23 | Tắt máy giữa phiên RPA đã qua commit point                | ✅                                          |
| S24 | Spam 1000 tin/phút vào chatbot                            | ✅                                          |
| S25 | Block giấu action irreversible không khai                 | ✅ reject                                   |
| S26 | Publisher block trả phí phá sản                           | ✅                                          |
| S27 | Khách chat đòi xóa dữ liệu (GDPR)                         | ✅ shredding                                |
| S28 | "AI không tự gửi email cho khách VIP"                     | ✅ criterion+policy                         |
| S29 | Hai bộ phận sửa cùng contract dùng chung                  | ✅                                          |
| S30 | Self-host mất điện giữa 200 task                          | ✅ replay                                   |
| S31 | Agency 40 client tách chất lượng theo client              | ✅ (workspace dimension)                    |
| S32 | Dùng ecoma phát triển chính ecoma                         | ✅                                          |
| S33 | Gộp 2 tenant (M&A)                                        | ⚠️ operational, ngoài trần                  |
| S34 | Regulator audit realtime                                  | ✅ projection                               |
| S35 | Role được lấp bởi process con                             | ✅ (filler `process` — sinh từ finding)     |
| S36 | A/B hai quy trình cạnh tranh bằng outcome                 | ✅ shadow process-filler                    |
| S37 | Air-gap quốc phòng                                        | ✅                                          |
| S38 | Escalation ping-pong vô hạn                               | ✅ chain tuyến tính                         |
| S39 | Người tự approve việc mình làm ở 2 role                   | ✅ (distinct_filler_from — sinh từ finding) |
| S40 | Chi phí model tăng vọt giữa tháng                         | ✅                                          |

## Phần 1b — Exploration full-run #1 (S41–S44, từ vùng mù đã khai)

| ID  | Kịch bản                                                                           | Verdict                                                                                                                 |
| --- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| S41 | Khách VIP hồ sơ memory dài + yêu cầu hoàn tiền lớn (memory×irreversible×external)  | ✅ memory scope theo party + sàn Gate + leakage-gate                                                                    |
| S42 | Process-filler lồng 2 cấp, instance con kẹt terminal-hold                          | ✅ compose: task cha treo → SLA cha bắn escalation riêng — không cần cơ chế xuyên cấp                                   |
| S43 | Block private của client A trong tenant agency đa workspace                        | ⚠️→✅ **finding: install thiếu scope workspace — đã vá Block §6**                                                       |
| S44 | Sync-response + leakage-gate dưới tải                                              | ✅ verifier máy trong time_budget; quá → fail/degrade                                                                   |
| S45 | KB kéo từ git/website + support chatbot (dogfood ecoma-docs); nguồn web bị sửa lén | ✅ composition thuần: trigger→extract→Gate→materialize; web-source Gate chặt hơn git; drift = hash mismatch → Violation |

## Phần 1c — Exploration full-run #2 (S46–S54) — 4 vùng mù khai ở full-run #1 + 5 sinh mới

| ID  | Kịch bản                                                                                                | Nguồn                       | Verdict                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S46 | Node attended × takeover × màn hình đang hiện secret; người trợ giúp **từ xa** xem cùng                 | vùng mù #1                  | ⚠️→✅ **finding major: live-view là consumer thứ 4 đứng ngoài chốt masking** — vá Sandbox §3 (live-view = projection Scene sạch; driver không hỗ trợ → attended-only) |
| S47 | Agency: distill memory từ 30 khách của client A → Knowledge; client B dùng chung tenant, khác workspace | vùng mù #2                  | ⚠️→✅ **finding major: distill không khai chiều workspace** — vá Memory §5 (mặc định workspace hẹp nhất) + Knowledge §1 (Collection có scope) + rubric C6             |
| S48 | Bootstrap admin tự cấp capability                                                                       | vùng mù #3                  | ✅ **accepted-by-owner** (chủ quyền tenant có audit) — không tính lại                                                                                                 |
| S49 | Publisher độc tự đẩy block của mình qua vòng review verified để lấy badge, rồi ship artifact `code`     | vùng mù #4                  | ⚠️→✅ **finding major: cửa duy nhất của artifact code không khai chống tự-duyệt** — vá Hub §7 (`distinct_filler_from` + `unverify` có event)                          |
| S50 | Khách đòi quên → shred; 2 tuần sau ops khôi phục backup từ trước đó để cứu sự cố                        | desk-sim P3 (mới)           | ⚠️→✅ **BLOCKER: backup hồi sinh dữ liệu đã shred** — vá Event Log §4 (khóa ngoài đường backup; escrow chịu cùng lệnh shred)                                          |
| S51 | `singleton: true` đóng sổ tháng; holder chết **sau** khi đã ghi effect ra hệ ngoài; TTL hết             | implementation-sketch (mới) | ⚠️→✅ **finding major: Working Data §3 nói "tự nhả", RPA NS §4 nói "không silent re-run"** — vá: lease `orphaned`, escalation terminal quyết                          |
| S52 | Agent filler gọi tool ngoài (giao thức kiểu MCP) có side-effect, không khai Effect ở Task               | nhóm O (mới)                | ⚠️→✅ **finding major: tool-call chưa có nhà** — vá Task §5 (hành vi-trong-filler vs Task; 2 biên cứng; tool protocol = adapter)                                      |
| S53 | Tenant ngừng trả tiền → suspend; 3 tháng sau đòi export rồi xóa sạch                                    | P-run (mới)                 | ⚠️→✅ **finding major: vòng đời tenant không có nhà** — vá Tenant §2b (provision→suspended→export→purge)                                                              |
| S54 | Release major đã migrate schema DataTable; phát hiện lỗi, cần quay lại                                  | P2/I7 (mới)                 | ⚠️→✅ **finding major: rollback không có nhà** — vá NS §8 (down-migration hoặc `irreversible_migration`) + Composition §4                                             |

## Phần 1d — Exploration full-run #3 / (S55–S62) — 5 vùng mù khai ở #2 + 3 sinh mới

| ID  | Kịch bản                                                                                                   | Nguồn                                           | Verdict                                                                                                                                                                                                                                                      |
| --- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S55 | Ổ cứng self-host chết; backup dữ liệu nguyên vẹn; root key **ngoài** đường backup theo đúng luật           | desk-sim P1 `backup/restore` (chiều ngược)      | ⚠️→✅ **BLOCKER mất toàn bộ dữ liệu dù backup nguyên** — vá Vault §3 ba-vế (DR bắt buộc cho root/tenant-DEK, subject key không có bản sao) + bootstrap thử-thách-checksum + FMEA; litmus Vault #6                                                            |
| S56 | Khách đòi quên → shred; ops khôi phục **snapshot của chính key store** (không phải backup dữ liệu)         | vùng mù `key rotation × log cũ` (khai ở #2)     | ⚠️→✅ **major S50 tái sinh ở cửa khác** — vá luật _loại bản sao_: chỉ replica tiến-lên-trước, cấm snapshot point-in-time key store (Vault §3(c)/§4/§6.2, Event Log §4) → sinh **P3b**                                                                        |
| S57 | Người dùng bấm **Approve** trên app desktop attended (Tauri, ADR-0005) khi máy đang offline với server     | desk-sim ADR→trần                               | ⚠️→✅ **major kênh nội-máy chưa có tên trong trần + nguy cơ đường ghi riêng của UI** — vá RPA NS §4 ba biên cứng; litmus RPA #10 (tắt kênh → runtime chạy đủ)                                                                                                |
| S58 | Test một process của tenant A: definition + fixture phải vào "tenant `test`"                               | implementation-sketch trên spec mới             | ⚠️→✅ **major tenant `test` không có nhà + buộc mở đường copy artifact xuyên tenant** (Artifact Store §4 cấm) — vá bằng **test run scope có nhãn**, 0 biên mới                                                                                               |
| S59 | Test run chạy `read` thật vào CRM của khách bằng credential production; effect ghi đã bị `forbidden`       | desk-sim F3 trên spec mới                       | ⚠️→✅ **major `forbidden` chỉ chặn _ghi_, không chặn _đọc_** — vá Vault §5: test run scope không resolve secret production                                                                                                                                   |
| S60 | Contract khai `test_behavior: dry_run` nhưng adapter mail không có chế độ dry-run                          | implementation-sketch                           | ⚠️→✅ **major đúng chỗ harness thề "không đoán" lại phải đoán** — vá `supports_dry_run` của adapter + resolve về `forbidden` + static analysis cặp contract×adapter                                                                                          |
| S61 | Dev cộng đồng gửi PR vào repo public                                                                       | P5 dòng _Contribution_                          | ⚠️→✅ **major charter khai _cổng_ (CLA) mà không khai _đường_** — vá ở playbook giao hàng                                                                                                                                                                    |
| S68 | Một người kéo card trên GitHub Projects sang cột "Done" cho việc chưa có exit-litmus nào pass              | ranh giới board                                 | ⚠️→✅ Board sở hữu **trạng thái**, không sở hữu **định nghĩa xong**; exit-litmus chỉ ở roadmap §4. Card không trace được về một ID = phạm vi chưa quyết ⇒ **PR sửa roadmap trước, không kéo card**                                                           |
| S69 | Ai đó thêm cột "Priority" trên board để sắp lại việc                                                       | ranh giới board                                 | ⚠️→✅ **Cấm**: §2 (thứ tự đáng làm) và §3b (điều kiện mở khóa) đã là hai nguồn; cột gõ tay là nguồn thứ ba và _gần tay nhất nên sẽ thắng cả hai_                                                                                                             |
| S64 | Giữ backup 5 năm trong khi support window là "major hiện tại + 1"                                          | giao thoa #1                                    | ⚠️→✅ Retention ≤ support window, hoặc khai **restore path có diễn tập**; vượt mà thiếu ⇒ **cảnh báo lúc khởi động**                                                                                                                                         |
| S65 | Cần lùi một bản nâng cấp **sau** khi cửa sổ rollback đã hết                                                | giao thoa #2                                    | ⚠️→✅ Đó **không phải rollback** — là restore+replay, mất dữ liệu từ điểm backup; thủ tục **cấm** gọi chung một tên                                                                                                                                          |
| S66 | Máy attended: node runtime train mới, lớp UI train cũ                                                      | giao thoa #3                                    | ⚠️→✅ Skew **nội-máy** — không handshake nào của hệ thấy; UI kiểm lúc bắt tay nội-máy và **từ chối chạy**; installer cũng từ chối bộ lệch                                                                                                                    |
| S67 | Đang nâng cuốn chiếu fleet server, một node chạm hai server khác train                                     | cluster-run                                     | ⚠️→✅ Tập protocol của fleet = **giao**; bỏ một protocol là hành động **toàn fleet**; cấm vá bằng cách pin node vào một server                                                                                                                               |
| S63 | Sau khi bỏ tenant `test`: một test run ghi DataTable rồi bảng production time-travel as-of log-position đó | phase 6 litmus pass                             | ⚠️→✅ **major nhãn `run_kind` không có nhà** — khai hệ quả ở 4 consumer nhưng không ở Event Log ⇒ projection viết sau quên lọc; vá Event Log §1/§3 (nhà canonical + luật "mọi projection khai lập trường") + Calibration §2 + Working Data §1                |
| S62 | Publisher đẩy block class `code` kèm suite riêng để lấy badge verified                                     | vùng mù `unverify × mirror` (khai ở #2), nhóm N | ⚠️→✅ **major vòng tròn "chạy code chưa verified để được verified"** — vá Hub §7 (bằng chứng phụ, test run scope, 0 secret) và đóng bởi Runtime Sandbox §6 — cắt vòng bằng cách biến an toàn thành thuộc tính của cái chuồng, không phải của code đang duyệt |

## Phần 1e — Vòng đối kháng cạnh tranh O3c (S70–S73, sinh từ battery đối thủ 2026-08-03)

| ID  | Kịch bản                                                                                                                        | Nguồn                          | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S70 | Tenant admin có cửa hậu DB **viết lại** một entry đã ghi — kể cả entry ghi override của chính họ                                | battery đối thủ (nhóm N)       | ⚠️→✅ **found, major — đã vá ở Event Log §4b** (ba tầng: hash-chain per stream, head ký, anchor ngoài với mặc định theo hình dạng triển khai — opt-in self-host, bật-mặc-định cloud với control-plane làm nhân chứng), §1 (`prev_hash` vào tuple trước khi ◆G0 freeze, hash trên dạng lưu trữ nên không đụng crypto-shredding) và §2 (tham chiếu xuyên stream mang hash). Restore hợp pháp phát một entry `restore` đã-neo để không bị nhầm với truncation. Giới hạn khai rõ: self-host đơn độc không anchor không đóng hoàn toàn được, vì đóng lỗ có tính quan hệ                                                                                                                                                                                                                   |
| S71 | Người duyệt phải **chọn 1 trong 3 phương án**, bước sau rẽ theo lựa chọn đó                                                     | battery đối thủ (suspend-step) | ✅ quyết định là **Task của Role người**, phê duyệt là Gate — lựa chọn là output artifact của decision-task đi tiếp qua Handoff; Gate không mang input tự do                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| S72 | Ứng dụng **redesign toàn bộ** giữa hai phiên; automation phải sống sót **và** tự rẻ trở lại                                     | battery đối thủ (RPA)          | ✅ tier 4 gánh phiên đầu, healing vá tier 1 có lineage + trust decay, distillation là chiều về — một trục, không phải lựa chọn per-product                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| S73 | Agent ngoài gọi vào qua A2A/ACP/MCP, và một task gọi ra một agent ngoài                                                         | battery đối thủ (chuẩn O4)     | ✅ chiều vào = Trigger + auth tại biên + external filler; chiều ra = adapter của agent runtime (Task §5) — không interface thứ ba mọc ra ở chiều nào                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| S74 | Agency mất một khách, phải export và xóa mọi thứ về brand đó — đơn vị là **workspace**, không phải tenant, không phải một Party | quét usecase (dimension model) | ⚠️ **found, major, CÒN MỞ**: Tenant §2b cho _tenant_ một lifecycle đầy đủ (provision → suspended → export → purge), nhưng §3 để workspace là vách mềm **không có lifecycle nào** — trong khi agency là một tenant với một workspace mỗi khách (đúng hình dạng beachhead). "Mọi thứ về client X" trải trên nhiều Party, một Knowledge Collection có scope, các ô calibration trên chiều workspace, hàng DataTable, block cài scope-workspace, và aggregate clickstream, và không cơ chế nào liệt kê hay đóng tập đó. Shred từng Party không bằng một export-and-purge workspace có Gate và trọn vẹn, nên lời hứa xóa theo hợp đồng với mỗi khách rời đi không có cơ chế chống lưng. Nhà ứng viên: một lifecycle của workspace bên cạnh của tenant, purge là một Task của Role có Gate |

## Phần 1f — Vòng vá desk-sim (S75–S85, từ quét usecase O3g)

| ID  | Kịch bản                                                                                                                                      | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S75 | Một người liên hệ phục vụ hai brand đối thủ; Party tự-khai gộp danh tính, và một memory observed cho brand A bị retrieve vào task của brand B | ⚠️→✅ **found, major (C6) — đã vá ở Memory §4**: cô lập cross-subject là chưa đủ; retrieval nay mặc định về các entry observed trong workspace của chính instance cộng `tenant_self` (workspace quan sát khôi phục từ provenance §2), pooling xuyên workspace là lựa chọn tường minh mà Tenant §3 cấp — cùng vách mềm áp theo chiều dọc tại lúc retrieval, không chỉ lúc distill (§5)                                                 |
| S76 | Một hồ sơ memory `confidential` chảy vào một task có Role được lấp bởi một model API ngoài                                                    | ⚠️→✅ **found, major — đã vá ở Knowledge §3 và Memory §2**: `model_policy` là thuộc tính của mức classification, không chỉ của một Collection, nên bất cứ thứ gì được classify khi vào context của model — một chunk, một memory entry, một field envelope — đều được định tuyến theo nó; mức `secret` không bao giờ vào một model ngoài. Điều này quản context _đi vào_ model, chiều mà egress guard không chặn                      |
| S77 | Node attended của một người rời đi thù địch giữ một `persistent_profile` với login khách còn sống trên laptop họ giữ vật lý                   | ⚠️→✅ **found, major — đã vá ở Sandbox & Credential §1**: thu hồi key của node chặn việc tương lai nhưng không chạm được cookie của chính hệ target trong profile; decommission vì thế đi kèm một compensation Task xoay credential của hệ target, và một credential không khai được coi là vẫn còn sống (xref RPA North Star §4)                                                                                                     |
| S78 | Nhân sự cloud ops mở một tenant khách để debug một process kẹt                                                                                | ⚠️→✅ **found, major — đã vá ở Tenant & Identity §2c**: support access là membership bên trong tenant khách — một support Role có Gate, giới hạn thời gian, mà mọi read là một read event trong log của chính tenant, tenant-visible; không đường infra-level nào đọc dữ liệu tenant mà không hiện ra ở đó. Claim là public, test thực thi sống trong `cloud/`                                                                        |
| S79 | Một Party bị shred và dashboard margin-per-client được rebuild                                                                                | ⚠️→✅ **found, major (G9) — đã vá ở Working Data §1 và Event Log §3**: định nghĩa table khai cột nào là subject-keyed; một shred render chúng thành một giá trị `shredded` tường minh trong mọi projection và mọi read time-travel, các field nghiệp vụ sống sót dưới tenant DEK, và rebuild-equivalence được scope về tập key đọc-được hiện tại — con số đã audit tính lại với delta giải thích bởi shred, không âm thầm             |
| S80 | Một tenant chạm trần storage và engine vẫn phải ghi được lý do vì sao một write bị từ chối                                                    | ⚠️→✅ **found, major — đã vá ở Quota & Scheduling §5**: accounting của chính engine — quyết định biên, Violation, escalation, entry lifecycle — không bao giờ chịu admission, nên entry giải thích "vì sao tôi bị chặn" luôn ghi được; admission gate các đơn vị của tenant, và "absolute" scope việc từ chối đơn vị kế tiếp, không phải đóng băng log (điều hòa §1 với §3)                                                           |
| S81 | Một engine major bỏ một path deprecated mà một pinned block của publisher đã biến mất còn dùng                                                | ⚠️→✅ **found, major (R7 tension) — đã vá ở Release & Compatibility §3 và Hub North Star §2**: "chạy mãi" được scope về train đã cài pin; projection `deprecated_used` đếm các block đã cài, và upgrade chạy lại static analysis của lockfile đối chiếu train mới, đưa một pin không tương thích thành một quyết định có Gate _trước_ cutover — không bao giờ là break runtime âm thầm, cũng không bao giờ là upgrade bị chặn âm thầm |
| S82 | Một người duyệt phía khách im lặng quá SLA và agency duyệt thay khách                                                                         | ⚠️→✅ **found, minor — đã vá ở Escalation §4**: reassign thay _ai_ duyệt, không bao giờ thay _một approval nghĩa là gì_; nơi process cần chính sự đồng thuận của bên ngoài, đường handler là `override_gate` (một chấp nhận trách nhiệm có ký) hoặc halt, nên "client sign-off" không thể âm thầm biến thành "agency sign-off" bên trong một Judgment trông bình thường                                                               |
| S83 | Client của một agency đòi xem mọi người đã chạm việc của họ quý này, không có account                                                         | ✅ client được gán vào một Role, không được cấp account (Tenant §7); Role mang các read grant scope về workspace của client (§4), portal là một Channel, và Org Work là một projection đọc-được dưới scope đó (Human Surface §2) — chính các read của client cũng là read event (§4)                                                                                                                                                  |
| S84 | Đội security của client đòi bằng chứng cô lập khỏi một brand đối thủ do cùng agency phục vụ                                                   | ✅ Tenant & Identity §3 viết sẵn câu trả lời trung thực nguyên văn — một workspace không phải biên security; câu đúng là "dữ liệu của bạn không rời khỏi installation của agency", và đường nâng cấp là một tenant thứ hai hoặc Cloud. Giữ làm giáp regression chống ai đó "đơn giản hóa" workspace thành một biên được tuyên                                                                                                         |
| S85 | Một khách chat gài "trivia" vô hại mong nó auto-pass memory Gate và tới được context của task sau                                             | ✅ retrieval chỉ phục vụ các entry đã qua Gate — một candidate thành một entry chỉ qua Gate (Memory §3, §5), không có store candidate tiền-Gate để chạm tới; claim gài rơi vào population đã-Gate nơi tự-khai của chính subject mang basis weight thấp, sampling audit dải auto-pass, và action nó nhắm được canh bởi một sàn Gate (S41)                                                                                              |

## Phần 1g — Vòng đối kháng cạnh tranh O5 (S86–S89, sinh từ battery đối thủ 2026-08-04)

| ID  | Kịch bản                                                                                                              | Nguồn                                | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S86 | Một workspace khách của agency tenant loop vì trigger hỏng, các workspace khách anh em bị bỏ đói                      | battery đối thủ (câu hỏi mở O5)      | ⚠️ **found, major, CÒN MỞ**: Quota & Scheduling §6 scope lời hứa công bằng đo được **giữa các tenant** — "no tenant is starved by another tenant's _behaviour_" — và §8 tuyên QoS nội-tenant là **non-goal tường minh** ("that is `priority`, Task §2"), nên đây là một lằn ranh chủ động va chạm với hình dạng beachhead, không phải sơ suất. Bên trong tenant, một hàng xóm chạy loạn ở `priority` ngang nhau đi thẳng qua — chính lập luận chống FIFO của §6 áp nguyên xuống một tầng. Cascade không có tầng workspace (`tenant → template → process → role → task`, §3), nên agency thậm chí không khai được trần per-client: Trigger `guard` chặn một cửa, Task `budget` một đơn vị, capacity của filler một chỗ ngồi — và bảng §0 tự nói vì sao cơ chế chặn-theo-nguồn không thay được trần-theo-người-tiêu-thụ, đúng nguyên văn tình huống nơi người tiêu thụ là workspace của client (S31, S74). Nhà ứng viên: workspace là một tầng tighten-only của cascade quota và một chiều của fair queue nội-tenant — đúng vách mềm Tenant §3 đã vẽ, áp sang scheduling |
| S87 | Người duyệt duy nhất đi vắng, SLA `awaiting_review` của Gate hết hạn trong khi việc dồn phía sau                      | battery đối thủ (Invariant 5, O5)    | ✅ quá deadline chỉ còn escalate hoặc halt — "never auto-pass on timeout" (Checkpoint §6), chain chạm đáy ở terminal handler bắt buộc với mặc định n=1 là `nudge → hold` (Escalation §3), và litmus #1 của Checkpoint hỏi đúng câu này. Giữ làm giáp regression vì hệ sinh thái xung quanh coi timer đua với approval là flow _tự đi tiếp_ theo mặc định — mọi tiện ích "unblock việc kẹt" tương lai phải đọc lại hàng này và invariant 5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| S88 | Kẻ ngoài biết được id của một review đang chờ, post một "resolve" vào đó — không Role, không danh tính                | battery đối thủ (ghế trống identity) | ✅ không có động từ resolve nào để bắn vào: Gate chỉ đi tiếp bằng cách tiêu thụ Judgment (Checkpoint §1), quyền tạo Judgment là capability `judge` trên một Role (§3), và mọi Judgment mang vĩnh viễn danh tính verifier đầy đủ — một pass vô danh không có schema để tồn tại. Bề mặt vào là Trigger với auth tại biên (S73), và cả đường khẩn cấp cũng là một Judgment `override_gate` có ký với `basis: override` (Escalation §4). Giữ làm giáp regression chống việc một endpoint resume-token trần được thêm vào vì tiện                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| S89 | Handler duyệt một action bị chặn, và cái gật đầu đơn lẻ đó âm thầm thành allow-rule thường trực không bao giờ hỏi lại | battery đối thủ (anti-pattern O5)    | ✅ cái gật bám vào instance, không bao giờ vào policy: `override_gate` và `absorb` đóng _escalation đó_ bằng một Judgment có ký trên _output đó_ (Escalation §4), và Judgment gắn vĩnh viễn vào output, không mở lại gì (Checkpoint §1). Một rule thường trực sẽ phải là thay đổi một Criterion có version (§1) hoặc một entry `config-change` (Event Log §1), đi qua Gate của chính nó; đường tự-nới duy nhất là calibration, đi qua pipeline đã hiệu chuẩn dưới sampling bắt buộc, mọi thay đổi ngưỡng phát một audit event đảo ngược được (§4, litmus #3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

## Phần 1h — Vòng bề mặt BI (S90–S96, sinh từ issue #119 + đối thủ bề mặt BI, 2026-08-04)

| ID  | Kịch bản                                                                                                                                                 | Nguồn                              | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S90 | Client của agency chất vấn một con số margin đã công bố, trong tay chỉ có read grant scope workspace của mình — con số phải tự chứng minh mà không mở sổ | issue #119 (số tự giải thích)      | ✅ provenance của một con số là `(log position, query text, result hash)` (Working Data §1) nên nó tự tái diễn chính xác; drill-down không phải một bề mặt đặc biệt mà là một read nữa dưới chính grant của Role người xem — static analysis kiểm các bảng chạm tới, và một join/aggregate kế thừa floor phân loại cao nhất của mọi bảng nó chạm, nên con số đã công bố qua leakage gate không bao giờ declassify các row bên dưới. Chính các drill-read của client cũng là read event (Tenant §4, S83). Giữ làm giáp đối chứng với mẫu của cả hạng BI: gắn quyền drill vào embedded viewer như một việc vá sau                                                                                                                                                                                                                                                                                                                                                                                   |
| S91 | Định nghĩa metric margin bị sửa giữa hai quý, và con số quý trước — đã báo cáo — được render lại cho hội đồng                                            | issue #119 (metric layer)          | ✅ định nghĩa metric/projection là một entity có id, version và lineage (Working Data §4), và một con số đã render pin `(position, query, hash)` — render lại tái tạo đúng con số cũ, và chỉ có hai đường được phép làm nó dịch chuyển: một version định nghĩa mới, với delta quy được bằng diff hai version tại cùng một position, và một cú shred, với delta được projection tự giải thích (Working Data §1). Tooling phát hiện drift trên nền entity có version là việc của roadmap (M5), không phải trần — không đối thủ nào trong vòng khảo sát giải thích được một con số đã dịch chuyển                                                                                                                                                                                                                                                                                                                                                                                                    |
| S92 | Một định nghĩa metric mang theo fixture test của chính nó, và chạy suite đó không được làm xê dịch con số production                                     | issue #119 (metric tự kiểm)        | ✅ write của fixture mang `run_kind: test` với nhà canonical ở Event Log §1/§3: bảng production không thấy nó, kể cả một read time-travel (Working Data §1), mọi projection khai tường minh lập trường với label — không có mặc định im lặng — và mỗi projection mang một negative test máy kiểm, thiếu nó thì bị chặn merge về mặt cấu trúc (Event Log §3). Đây chính là cơ chế mà metric-tự-kiểm cưỡi lên — không cần luật mới                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| S93 | Agency cài một gói dashboard khách từ Hub cho workspace của một client, và client — external filler không account — mở nó                                | issue #119 (dashboard là artifact) | ✅ thuần composition của các hàng đã chốt: install khai `scope: workspace` (Block §6, S43); định nghĩa metric là entity có version chính là điều làm gói dashboard thành một block type "as a matter of course" (Working Data §4); nội dung render egress theo classification áp nguyên (§4); và client xem qua một Role với read grant scope workspace, chính các read đó cũng là read event (Tenant §7/§4, S83)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| S94 | Alert margin hằng đêm nổ lúc 02:00 khi không ai thức, rồi metric dập dềnh quanh ngưỡng suốt một tuần                                                     | issue #119 (alerting)              | ✅ trigger `schedule` xác thực như mọi trigger (`tenant_session`, Trigger §2); cú vượt ngưỡng raise một escalation — taxonomy trigger tường minh là mở, nên `metric_threshold` không cần đổi engine (Escalation §2) — và escalation là một Task của một Role chạm đáy ở terminal handler, không bao giờ là một ping bắn-rồi-quên (§3); notification feed chỉ render các escalation entry (§8, Event Log §3); và tuần dập dềnh được storm control hấp thụ — dedup khi một cái đang mở, correlation về một root cause, hàng đợi attention xếp theo irreversibility, priority và tuổi (§5). Giữ làm giáp đối chứng với hình dạng alert của cả hạng: một message kênh không ai nhận, không dedup, không trạng thái kết thúc                                                                                                                                                                                                                                                                           |
| S95 | Mọi con số trên dashboard hướng client render kèm một confidence dẫn xuất từ calibration, và một Role trong bảng phân rã chỉ có một filler người         | issue #119 (confidence trên số)    | ⚠️→✅ **found, major: calibration là nguồn duy nhất có phân loại về bản chất mà vắng khỏi lattice** — `view_calibration` gác cell, còn một con số _dẫn xuất_ từ cell không phải cell nên không kế thừa floor và không để lại read event. Vá ở Calibration §6 bằng cách **gia nhập lattice sẵn có** (cell mang classification, không khai là `confidential`; canonical Knowledge §3): floor propagation vào leakage gate (Working Data §1), group floor chống tái-định-danh (Memory §5), read event theo mức (Tenant §4) và định tuyến `model_policy` — **không cơ chế mới nào**. Cố ý không vá: không floor aggregation thứ hai (Memory §5 là nhà, Clickstream §7 đã từ chối một checkpoint thứ hai) và không Gate declassification mới (một scalar không cho leakage criterion gì để phán). Mặc định vẫn từ chối confidence hướng client — nới nó vẫn là quyết định `calibration_visibility_policy`                                                                                              |
| S96 | Một data subject bị shred, và bản export tháng trước đã giao các row của họ vào warehouse của chính khách hàng                                           | issue #119 (export CDC)            | ⚠️ **found, minor, CÒN MỞ**: bên trong installation cú shred kín kẽ — projection render `shredded`, rebuild-equivalence scope về tập key còn đọc được (Event Log §3/§4, S79), và lần export kế tiếp re-project đúng như vậy — nhưng trần im lặng về bản copy đã rời đi. Contract export nêu egress-theo-classification tại thời điểm rời (Working Data §4, Tenant §2b) và không nói gì về erasure sau đó; câu "a backup is not an erasure blind spot" của Tenant §6 chỉ phủ backup. Hai adapter trung thực rẽ đôi: exporter kiểu snapshot không bao giờ xóa phía dưới, exporter kiểu log-replay tự nhiên mang shred entry như một tombstone. Minor vì cột subject-keyed chỉ băng biên qua đúng cổng classification tường minh; mảnh thiếu là một limb trên contract export — shred entry là một phần của stream được export, và adapter khai có áp erasure phía dưới hay không, lời khai đó cân được trong quyết định egress cho cột subject-keyed. Nhà ứng viên: Working Data §4 cộng một litmus |

## Phần 1i — Vòng harness agent ngoài (S97–S103, sinh từ đối kháng hạng coding-agent, 2026-08-04)

| ID   | Kịch bản                                                                                                                                        | Nguồn                               | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S97  | Một agent filler gọi tên model do nhà cung cấp host, và nhà cung cấp trỏ lại tên đó sang trọng số mới hoặc đổi harness phía server mà không báo | đối kháng hạng harness (E1)         | ⚠️→✅ **found, major, đã thu hẹp qua đối kháng**: khung ban đầu — buộc danh tính ở Filler interface trước ◆G1 — không sống sót. Provider drift chính là lý do Calibration §4 đã **ép** half-life tồn tại; "Model drift → verifier identity gồm version + `config_hash`" là quyết định đã chốt (Checkpoint §8); sampling + giáng tức thì đóng vòng trên hành vi quan sát được nên profile không lặng lẽ trôi tới `autonomous`; và "adapter tự khai model không-pin-được" đúng là nhãn tự-khai mà Quota §4 gọi là honour system. Thứ sống sót là **bản ghi**: entry nêu model **được yêu cầu**, chưa bao giờ nêu model **được phục vụ**. Vá ở Task §4 theo đúng mẫu bù trừ của Knowledge §4, cộng Role §3 (adapter resolve tên trôi phải phản ánh vào `config_hash`; trần tier nếu muốn là giá trị `graduation_policy`, không phải luật engine). **Không dính ◆G1**: scope của `conformance-g1` là Filler interface + Session effect, không mảnh nào rơi vào đó                                                                                                                          |
| S98  | Một con người vừa đứng sau agent filler tạo ra output vừa giữ `judge` trên chính output đó                                                      | đối kháng hạng harness (2b thu hẹp) | ⚠️→✅ **found, major — nâng từ minor**: `distinct_filler_from` so filler, nên người đặt một agent vào giữa mình và công việc nhận được bảo đảm mà người làm trực tiếp không có — một nhánh `if human` đến dưới dạng lỗ, ngược litmus #3 của Role ("mọi đường, kể cả qua hai role"), và cùng hình dạng với S49 vốn xếp major. Vá ở Checkpoint §2 bằng `basis` được khai: `filler` (mặc định, đúng hành vi hiện nay) hoặc `principal` — principal chịu trách nhiệm mà Tenant §4 vốn đã ghi vì `grant_capability` là lao động có Gate; cạnh đó đã tồn tại, chỉ là phép so không đọc được. Cùng bản vá nói thẳng tầm với: guard ràng stage/assignment và **không** nới sang `override_gate`/`arbiter` — nơi phán xử đã hỏng                                                                                                                                                                                                                                                                                                                                                                |
| S99  | Coding agent của chính một member mang memory mức tài khoản từ việc của tenant này sang phiên của tenant khác                                   | đối kháng hạng harness (E6)         | ⚠️ **found, major, CÒN MỞ — xếp lại thành triệu chứng của S97 thay vì một bản vá riêng**: `model_policy` là thuộc tính của **mức** phân loại, còn "đích này biết nhớ" là thuộc tính của **đích** — treo nó lên mức nghĩa là khai một lần _cho mỗi mức_, nên tenant vá `confidential` thì `internal` vẫn hở và mỗi mức mới là một chỗ mới để quên: đúng lỗi `run_kind` mà E5 gọi tên. Lời khai thuộc về adapter, cạnh lời khai của S97, và `model_policy` **đọc** nó ở đó. Hai vế cũng đã bị nói quá: invariant 4 ràng buộc hệ và operator, một store do nhà cung cấp giữ nằm ngoài cả hai — loại suy đúng nhất là một filler người làm cho hai tenant, thứ invariant chưa bao giờ tuyên với tới. Cái còn nợ là **giới hạn trung thực** theo giọng `persistent_profile` (Sandbox §1), theo **cả hai chiều**                                                                                                                                                                                                                                                                             |
| S100 | Năng lực đằng sau một agent filler ngoài cạn vì trần phía nhà cung cấp, và trần đó nhiều ngày mới reset                                         | đối kháng hạng harness (E5)         | ⚠️→✅ **found, minor**: máy móc đứng vững — không auto-pass, attempt có biên, storm control gộp lặp — và thứ thiếu là một **giá trị**, không phải một limb. Vá ở Role §3, nơi availability vốn đã mang ngày cho nghỉ phép và schema dùng chung cho cả ba loại filler: availability khai được chân trời, không khai thì `unavailable` nghĩa là _bây giờ_ chứ không phải _cho tới khi_; Escalation §2 mang nó theo để handler thấy. Nói ngay trong cùng câu vì nếu không bản vá sẽ đẻ ra một cú stall: chân trời **chú thích, không bao giờ treo**. Cố ý từ chối: đọc nó ở admission của quota — thứ sẽ trộn hai trục Quota §0 sinh ra để tách                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| S101 | Hội đồng lao động hỏi hệ thống có đánh giá nhân sự không, và một nhân viên đòi xem — rồi xóa — thứ hệ thống giữ về mình                         | đối kháng hạng harness (E4)         | ⚠️ **một phần đã trả lời, một phần bị từ chối, một vế cố ý CÒN MỞ**. **Trả lời miễn phí** nhờ bản vá S95: "ai đã đọc gì về tôi" chính là read event theo mức (Tenant §4), vốn bất động chỉ vì calibration nằm ngoài lattice. **Từ chối** — phần dư erasure: "work product thuộc về tenant — artifact, judgment" (Tenant §6) và cell là projection của chúng (Calibration §0), nên một lịch sử giả danh sống lâu hơn con người là suy ra một bước từ văn bản đã khai, thứ constitution cấm tính là finding. **Từ chối ở nhà được đề xuất** — một đường đọc-về-chính-mình là grant scope theo danh tính người đọc, tức một chiều scoping mới trong hệ quyền duy nhất, và litmus #5 của Tenant §4 mời đúng phản chứng đó. **CÒN MỞ**: công-bố-mặc-định hay công-bố-khi-hỏi là policy của deployer                                                                                                                                                                                                                                                                                         |
| S102 | Cả đội cùng chạy coding agent trong một không gian chung, và agent của hai member dùng cùng model sau cùng một harness                          | đối kháng hạng harness (giáp)       | ✅ không gian chung là máy móc thường ngày: một runtime ngoài đăng ký Filler qua đúng schema đó và hành động của nó đi như Session effect (Role §3, Handoff §8), giao thức harness là một adapter mang identity và version chứ không phải interface thứ ba (Task §5, S73), và việc đến tay một agent qua một Role có `assignment_policy` — không qua tư cách thành viên của một phòng. Phân biệt hai agent là câu hỏi **cấu hình**, không phải câu hỏi danh tính: nơi harness khác nhau theo cách làm đổi hành vi — tool, system prompt, hạng model — chúng khác `config_hash` và vốn đã là hai filler; nơi không khác, chúng là một filler và gộp bằng chứng của chúng là đo đúng chứ không phải một cú sụp. Giữ làm giáp regression chống việc một khái niệm "thành viên đội" mọc lên cạnh Role — một agent là thành viên thay vì là filler chính là bất đối xứng litmus #1 cấm                                                                                                                                                                                                      |
| S103 | Một credential nhà cung cấp gánh cho toàn bộ agent của đội, xoay vòng qua nhiều tài khoản để giữ throughput                                     | đối kháng hạng harness (định vị)    | ✅ **cố-ý-không-thay, có-án-văn** — nền là kế toán đầu-cuối, đúng nền của lằn RPA giữa giải-một-thử-thách-được-trình-ra và kỹ-nghệ-hóa-không-bị-phát-hiện (RPA North Star non-goals, Sandbox & Credential §4). Xoay vòng credential để nâng throughput tồn tại **để** làm hồ sơ của bên kia quy sai công việc: log của một tài khoản hiện một khối lượng và một hình dạng mà không người dùng được ủy quyền đơn lẻ nào tạo ra. Đó là đầu xa của "mọi hành động có trách nhiệm giải trình" gãy, đúng như nó gãy khi automation được kỹ nghệ hóa cho giống người — một lý-do-cơ-chế chứ không phải khẩu vị, và nửa sổ sách của chính ecoma vẫn nguyên trong cả hai trường hợp, đó chính là điều làm lập luận này nói về đầu xa chứ không phải đầu này. Nó land đúng chỗ phán quyết kia đã land chứ không nghiêm hơn: không tự viết first-party, không marketing, với tới được như một adapter class `code` opt-in và tenant gánh phơi nhiễm — trong khi đường được-cấp-quyền-đúng-cách, một credential nhà cung cấp phát cho tổ chức đúng cho mục đích này, là đường engine hỗ trợ thẳng |
| S104 | Nhà cung cấp trỏ lại chính model đang làm **verifier**, nên thước đo và vật được đo dịch chuyển cùng lúc, cùng chiều                            | đối kháng vòng quyết (E1)           | ⚠️ **found, major, CÒN MỞ**: mọi cơ chế trả lời một filler trôi đều **chạy trên verifier** — sampling ở dải auto-pass, một cú reject hạ T_high, giáng tức thì (Checkpoint §4, Role §5) — nên khi cái trôi là verifier thì vòng lặp đo bằng một cây thước đang dịch và **không báo gì cả**. Shadow cũng không cứu: nó so primary với shadow tại cùng thời điểm, cả hai cùng của nhà cung cấp đó thì cả hai cùng dịch. Checkpoint §2 đã cảnh báo nhiều model cùng họ chấm một criterion ≈ một model, nhưng đó là cảnh báo lúc **cấu hình**; một cú re-point là đúng tương quan đó đến lúc **runtime**, và không ai canh. Nhà ứng viên: nới đoạn correlated-error từ configuration-time sang runtime, đọc bản ghi danh-tính-được-phục-vụ mà S97 vừa đặt vào Attempt                                                                                                                                                                                                                                                                                                                       |
| S105 | Một auditor đọc "not an HR performance tool" trong trần, rồi hỏi vì sao chính hệ đó phân việc theo filler và chấm điểm người làm                | đối kháng vòng quyết                | ⚠️ **found, major, CÒN MỞ**: non-goal ở Calibration §7 là lời khai về **mục đích**, trong khi §6 cộng Role §5 mô tả một cơ chế tích chất lượng theo từng người rồi thăng/giáng trên đó. Người đọc bên ngoài không kiểm được mục đích; họ kiểm **hành vi**. Corpus vốn cẩn thận đúng chỗ này — "workspace **không phải** biên an ninh — nói thẳng, ở đây và trên mọi trang bán hàng" (Tenant §3) là hình dạng một lời khai khi câu trung thực có giá. Câu hỏi không phải non-goal có đáng mong muốn không, mà nó có **đúng như đang viết** không; nếu không thì thứ phải sửa là **câu chữ**, không phải cơ chế — cơ chế lành, phạm vi chặt, đã gác visibility. Nhà ứng viên: viết lại Calibration §7 theo giọng Tenant §3. Để MỞ vì sửa một non-goal là lời hứa với người đọc, thuộc quyền owner                                                                                                                                                                                                                                                                                        |

## Phần 4 — Coverage gaps theo dimension model (vùng mù mới sau full-run #2)

Bốn vùng mù của full-run #1 **đã đóng** (S46, S47, S49 thành finding có vá; S48 accepted). Vùng mù mới khai báo:

`key rotation × evidence/log cũ còn phải đọc được` · `unverify × air-gap mirror (mirror không nhận tín hiệu thu hồi — độ trễ tin cậy)` · `sub-actor hỏng lặp lại × calibration bám filler (mù nguyên nhân?)` · `tenant purge × lockfile/entitlement của block đã mua` · `suspended tenant × node đang giữ session dở`.

**Sau full-run #3** — 3/5 vùng mù trên đã sinh finding có vá (S56 từ `key rotation`, S62 từ `unverify`); 2 vùng còn mở. Vùng mù **mới** khai báo:

`tag release khi trunk private và cây public lệch nhịp` · `UI attended offline × Action Item đã claim (lease hết TTL khi máy mất mạng)` · `migrations[].down của block × instance đang chạy pin digest cũ`.

**Hai vùng mù đã ĐÓNG bằng văn bản sẵn có** (M3 — một blind spot đã được trả lời mà vẫn để mở là đúng loại stale phương pháp cảnh báo):

- _test run scope × time-travel query_ — trả lời ở Working Data §1: "neither does a time-travel read as of a log position on a production table" thấy write của test (vá S63).
- _`environment: test` × pool filler của Role_ — trả lời ở Role §3 như engine law: filler `environment: test` "is not eligible for a production task, whatever its tier."
